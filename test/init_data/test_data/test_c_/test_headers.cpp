#include "test_headers.h"
int main()
{
	int *c = new int(1);
	int *b;
	char *m = new char(1);
	delete m;
    sum();
    sum();
	bool flag = temp(100);
	printf("flag: %d\n", flag);
    xxx(1223);
    return 0;
}